{
  "name": "fake-twitter",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "bsb -make-world && fpack build --dev ./lib/js/src/Index.js",
    "clean": "bsb -clean-world",
    "serve": "npx http-server . -c-1",
    "query": "yarn send-introspection-query"
  },
  "dependencies": {
    "reason-react": "^0.7.0",
    "reason-urql": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "bs-platform": "^5.0.6",
    "fastpack": "^0.9.1",
    "graphql_ppx": "^0.2.8"
  }
}
